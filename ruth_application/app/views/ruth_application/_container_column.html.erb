<% if RuthApplication.layout[:container][id] %>
	<% RuthApplication.layout[:container][id].each do |component| %>
		<% if component == :yield %>
			<div class="content-container">
				<%= yield %>
			</div>
		<% else %>
			<%= render partial: "ruth_application/component", locals: { component: component.to_s } %>
		<% end %>
	<% end %>
<% end %>