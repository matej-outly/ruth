function fancybox_ready()
{
	$(".fancybox").fancybox({
		openEffect	: 'none',
		closeEffect	: 'none'
	});
}
$(document).on('<%= RuthAdmin.use_turbolinks ? "turbolinks:load" : "ready" %>', fancybox_ready);
